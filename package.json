{
  "name": "amap-notes",
  "version": "0.1.3",
  "private": true,
  "description": "Local-first personal map annotation extension for AMap",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "serve": "python3 -m http.server 8765 --bind 127.0.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
