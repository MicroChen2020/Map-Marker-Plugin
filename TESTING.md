# v0.1.3 验证记录

验证日期：2026-09-18 至 2026-09-19。

- 28 项 Node 测试通过：包含原有 25 项及 3 项管理窗口 / 扩展消息路由测试。
- 后台 mock 测试覆盖同时请求只开一个窗口、后台重启后复用既有管理窗口、仅转发允许的事件且清理断开的连接。
- 浏览器真实 IndexedDB 通过 11 项检查：版本增长、过期覆盖拒绝、同时写入只允许一个成功、项目移入回收站、照片字节保留、恢复原编号、禁止直接永久删除正常项目、永久删除及旧副本不能复活。
- 双页面模拟地图实测：管理窗口改名同步地图、定位移动地图、地图点击“游览起点 150”同步选中表格和详情。
- 150 条虚构标注实测：分类筛选得到 30 条酒店；按名称排序；详情查看、项目栏折叠与详情关闭；页面高度保持 720px，表格在自己的 371px 区域内滚动，未撑长整页。
- 项目界面实测：新建、移入回收站、自动切换、恢复；独立 localhost 测试库的最后一个项目删除后显示空状态，刷新后保持空状态。
- 永久删除界面已检查二次确认；最终点击被自动审批拦截，未完成此按钮的端到端验收。数据库层永久删除和防复活检查已通过，测试项目仍留在回收站。
- 新版仍沿用原数据库与备份格式。原 v0.1.2 测试项目及照片项目已正常载入。
- `manager-preview.png` 是管理页面的虚构测试数据截图。

尚待真实扩展环境验证：Chrome / Edge 安装后的独立窗口创建与复用、后台端口在长期休眠后的恢复，以及当前高德网页的完整端到端使用。开发环境以 BroadcastChannel 验证同步，后台扩展端口另由 mock 测试覆盖，两者不能代替真实安装验收。多地图页同时操作、万级表格性能尚未专项验收。

以下保留此前版本记录。

# v0.1.2 验证记录

验证日期：2026-09-18。

- 25 项 Node 自动化测试通过，扩展 JavaScript 语法检查通过。
- 新增 8 项测试覆盖米 / 公里解析、地理半径、无效图形、整体移动、节点最小数量、分类迁移及删除。分类测试检查照片保留、原快照不变及最后一个分类保护。
- 本地模拟地图实测：区域虚线预览、点击起点闭合；双击完成两节点线段，没有重复节点；Enter 完成、Backspace 退回节点、Esc 取消。
- 本地模拟地图实测：圆心与边界两次点击成圈；输入“1 公里”后保存为 1000 米。
- 本地模拟地图实测：拖动区域节点、点击中点插入节点、删除节点、完成编辑及整次撤销；整体移动后取消保持原坐标。
- 本地模拟地图实测：搜索隐藏全部标注后，分类删除仍准确统计 3 个标注；迁移分类并撤销；二次确认连同 3 个标注删除后为 0 个对象，撤销后恢复 3 个对象及分类。照片恢复由数据层测试覆盖。
- `preview.png` 为模拟地图中的区域编辑界面，非真实高德截图。
- 以上交互使用与扩展共用的脚本；真实 Chrome / Edge 安装后的高德网页、地图平移快捷键与圆形控制点拖动仍需实际使用验收。

以下保留此前版本记录。

# v0.1.1 验证记录

验证日期：2026-09-17。

- 17 项 Node 自动化测试通过（原有 12 项 + 5 项标签控制测试）。
- 新测试覆盖 POI 隐藏与恢复、不打开原本关闭的兴趣点、showLabel 初始化选项、设置写入后才刷新、地图重建后应用 POI 偏好。
- 本地模拟地图实测：工作台从 x=810 拖动到 x=340；刷新后位置仍为 x=340 / y=88；再拖至 x=540 / y=150，横向和纵向移动均生效。
- 本地模拟地图实测：即时兴趣点开关隐藏底图测试标签，插件自有 6 个图形仍显示。
- 本地模拟地图实测：保存并刷新后，完整文字隐藏设置恢复，插件自有 6 个图形仍显示。
- 实际 Chrome 扩展的 chrome.storage 持久化、官网 SDK 标签显示与原生截图仍需安装后的真实高德验收。测试页使用 localStorage 模拟布局偏好存储，没有把模拟验证表述为官网验证。

以下保留 v0.1.0 的验证记录。

# v0.1.0 验证记录

验证日期：2026-09-16。

## 自动化检查

运行 `npm test`：12 项通过，0 项失败。所有扩展 JavaScript 文件通过 `node --check`。

- 完整备份恢复：标注编号、字段、照片字节、分类、几何信息保留；新建独立项目编号。
- CSV 往返：逗号、引号、多行文字、JSON 字段与公式文本保护。
- 按编号更新：照片保留，可选空值保留。
- 无效坐标、错误坐标系、重复编号、非法 CSV 拒绝导入。
- 自定义列映射和分类创建。
- 拒绝可执行附件格式、无效图形、危险字段名和重复分类编号。
- 分类显隐及全文字段筛选。
- 地理半径圈闭合和半径精度。
- 非点位对象不混入点位 CSV。
- 模拟 AMap 构造器延迟加载后的实例接入。
- 地图容器偏移下的屏幕/地理坐标双向换算。
- 地图桥接拒绝非目标来源和无效坐标。

## 本地浏览器交互验证

使用 `dev/` 模拟地图，运行与插件共用的 core、bridge、content、panel、store 代码。

已实际操作：

- 工作台显示、地图连接状态、地图点击新增点位。
- 点位名称与分类修改、本地保存、刷新后恢复。
- 三顶点区域绘制与完成操作。
- 地图缩放后覆盖层投影更新；过程中修复面板滚动影响外层页面的问题，并加入页面滚动后的重投影。
- PNG 本地照片解码压缩、添加、刷新后仍显示 1 张照片。
- 无效图片解码失败提示。
- 通过文件选择器恢复附带完整示例项目，预览 6 个标注并创建独立项目。
- 通过文件选择器导入示例 CSV、列映射、预览“新增 0 个，更新 2 个”、确认导入成功。
- 最终界面截图保存在 `preview.png`。该图使用模拟底图，不能代表真实高德插件已安装通过。

## 尚未验证

- 把扩展安装到真实 Chrome / Edge 后，在当前高德新旧网页上完成端到端验收。
- `chrome.tabs.captureVisibleTab` 在真实扩展弹窗中的 PNG 下载与临时隐藏面板。
- 万级点位、照片数量上限、存储接近配额时的性能。
- 多标签页版本冲突的浏览器端自动化复现（实现包含事务内版本检查，仍需专项测试）。
- 3D 地图姿态、卫星底图、官网导航后地图实例替换的长期稳定性。

当前工具环境可控制网页，但未提供加载自定义 Chrome 扩展的接口。因此未把模拟地图测试表述为真实扩展验收，也未改动用户浏览器的扩展安装设置。

## 真实高德首轮验收

1. 加载 `extension/`，刷新高德，确认工作台显示“高德已连接”。
2. 在已知建筑附近标点、画线和区域；拖动及缩放地图，确认对象贴合原地理位置。
3. 修改资料、添加照片，刷新网页检查恢复。
4. 导出完整备份，恢复为新项目，检查照片与图形。
5. 导出 CSV 修改名称后导入，确认只更新、不新增重复点位。
6. 点击插件图标导出 PNG，检查标注、图例与高德署名完整。

若第 1 步失败，停止绘图验收，保留现有数据并调整地图适配器。第一轮建议先用示例或少量非关键数据。
